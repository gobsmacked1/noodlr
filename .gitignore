# Dependencies
node_modules/

# Build output
dist/
.test-build/

# Fetched offline embedding model (bundled into the release zip, not committed)
models/

# Editor / OS
.DS_Store
Thumbs.db
*.log

# Local env / secrets (never commit)
.env
.env.*
!.env.example

# Foundry packaging artifacts
*.zip

# Scratch notes handed to `gh release create --notes-file` / `git commit -F`; the real record is
# changelog.md and the commit itself. PowerShell has no heredoc, so these files are the way a
# multi-line message is passed at all.
release-notes-*.md
.release-notes.md
.commit-msg.txt
